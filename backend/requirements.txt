# ── DocuBrain Backend — Minimal Production Dependencies ──────────────────────
# Purged from 186 → 17 direct packages after deep dependency audit.
# All transitive deps (grpcio, protobuf, etc.) are pulled in automatically.

# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi==0.124.0
uvicorn==0.38.0
python-multipart                    # Required for FastAPI UploadFile

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy                          # ORM (async + sync engines)
asyncpg                             # Async Postgres driver (FastAPI)
psycopg2-binary                     # Sync Postgres driver (Celery worker)

# ── Caching & Task Queue ─────────────────────────────────────────────────────
redis                               # App-level cache + Celery broker
celery                              # Background task processing

# ── Object Storage ────────────────────────────────────────────────────────────
minio                               # S3-compatible client (MinIO / AWS S3)

# ── AI / RAG Pipeline ────────────────────────────────────────────────────────
google-generativeai                 # Gemini Embeddings + GenAI SDK
langchain-google-genai              # LangChain ↔ Gemini chat integration
pinecone                            # Vector database client

# ── PDF Processing ────────────────────────────────────────────────────────────
pypdf                               # PDF text extraction (lightweight)

# ── Auth & Security ──────────────────────────────────────────────────────────
passlib==1.7.4                      # Password hashing
bcrypt==4.0.1                       # bcrypt backend for passlib
python-jose[cryptography]           # JWT encode/decode
python-magic                        # MIME type detection (file upload security)

# ── Validation ────────────────────────────────────────────────────────────────
email-validator                     # Pydantic EmailStr support
pydantic                            # Already a FastAPI dep, pinned for clarity

# ── Async ─────────────────────────────────────────────────────────────────────
anyio                               # Thread executor for sync SDKs
python-dotenv                       # .env file loading
