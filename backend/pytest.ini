[pytest]
# asyncio_mode = auto: every async test function is automatically treated as
# an async test — no need to decorate each one with @pytest.mark.asyncio.
asyncio_mode = auto

testpaths = tests

# Short traceback format — full tracebacks are noisy in CI logs.
# Use "long" locally when debugging a specific failure.
addopts = --tb=short -q
